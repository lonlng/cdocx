# CDocx 文档索引

## 文档列表

### 架构与进度文档

| 文档 | 说明 | 状态 |
|------|------|------|
| [架构设计.md](架构设计.md) | v2.0 全新架构设计 | 已完成 |
| [API示例.md](API示例.md) | v2.0 API 使用示例 | 已完成 |
| [增强路线图.md](增强路线图.md) | 功能增强路线图 | 已完成 |
| [项目进度报告.md](项目进度报告.md) | 项目进度与代码分析 | 已完成 |

### 工具文档

| 文档 | 说明 | 状态 |
|------|------|------|
| [Template Analyzer](../tools/template_analyzer/README.md) | Docx 模板分析器：自动提取占位符/书签/MERGEFIELD 并生成 C++ 头文件 | 已完成 |

### 技术参考文档（根目录）

| 文档 | 说明 |
|------|------|
| `README.md` | 项目简介与快速开始 |
| `AGENTS.md` | 完整 API 文档与架构说明 |
| `CLAUDE.md` | Claude Code 工作指南 |
| `CONTRIBUTING.md` | 贡献指南 |
| `STYLE.md` | 代码风格规范 |
| `编码规范.md` | 编码规范快速参考（中文） |
| `AI_PROMPT.txt` | AI 代码生成规范 |

### 头文件（已完成设计）

| 文件 | 说明 | 状态 |
|------|------|------|
| `include/cdocx.h` | 主聚合头文件 | 已更新 |
| `include/cdocx/enums.h` | 所有枚举类型 | 已创建 |
| `include/cdocx/format.h` | 格式属性类 | 已创建 |
| `include/cdocx/node.h` | 节点基类 | 已创建 |
| `include/cdocx/document.h` | 文档类 | 已创建 |
| `include/cdocx/section.h` | 节和页眉页脚 | 已创建 |
| `include/cdocx/body.h` | 文档正文 | 已创建 |
| `include/cdocx/paragraph.h` | 段落和 Run | 已创建 |
| `include/cdocx/table.h` | 表格类 | 已创建 |

---

## 快速导航

### 如果你是开发者

1. **了解新架构** → [架构设计.md](架构设计.md)
2. **查看 API 示例** → [API示例.md](API示例.md)
3. **查看根目录 README** → [README.md](../README.md)
4. **查看构建与测试指南** → [CLAUDE.md](../CLAUDE.md)
5. **查看增强路线图** → [增强路线图.md](增强路线图.md)

### 如果你是项目管理者

1. **查看项目进度** → [项目进度报告.md](项目进度报告.md)
2. **查看增强路线图** → [增强路线图.md](增强路线图.md)
3. **查看架构设计** → [架构设计.md](架构设计.md)

---

## v0.8.0 核心特性

### 设计亮点

- **DOM 风格 API** - 类似浏览器文档模型的 `Node`/`CompositeNode` 层次结构
- **CDocx 工厂类** - 便捷的文档创建/加载/保存工厂方法
- **类型安全** - 枚举替代字符串参数
- **链式调用** - 流畅的 `DocumentBuilder`、`ParagraphBuilder`、`TableBuilder` API
- **统一模板引擎** - `TemplateEngine` 字典风格模板填充，支持占位符和书签
- **现代 C++** - `shared_ptr`, 范围 for, 移动语义
- **访问者模式** - 灵活的文档遍历操作
- **属性保留** - DOM-XML 往返保留未建模属性，确保 Word 兼容性

### API 对比示例

```cpp
// 旧版 API (Legacy Iterator)
cdocx::Document doc("file.docx");
doc.open();
auto para = doc.paragraphs();
para.add_run("Hello", cdocx::kBold);
doc.save();

// 新版 API (DOM + 工厂)
auto doc = cdocx::CDocx::load_document("file.docx");
auto para = doc->get_body()->append_paragraph("Hello");
para->append_run(" World")
    ->get_font()
    .bold = true;
para->append_run(" Red text")
    ->get_font()
    .color = cdocx::Color::red();
cdocx::CDocx::save_document(*doc, "output.docx");
```

---

## 实现状态

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 1 | 基础设施 (format, node, convert_util) | 已完成 |
| Phase 2 | 文档结构 (document, section, body) | 已完成 |
| Phase 3 | 内容操作 (paragraph, table, builder) | 已完成 |
| Phase 4 | XML 序列化 (含 Field/Hyperlink/Bookmark) | 核心已完成 |
| Phase 5 | 高级功能 (search, mail_merge, watermark, comment, style) | 已完成 |
| Phase 6 | 兼容层与遗留 API 维护 | 进行中 |

---

## 功能需求与实现状态

CDocx 的核心目标是对 `.docx` 文件进行**增、改、查**。项目提供 **DOM API**（高抽象，无需了解 XML）和 **XML Parts API**（直接操作 Open XML 部件）两条路径，同时保留 **Legacy Iterator API** 作为兼容层。

### 使用路径说明

- **DOM API**：通过 `Node` / `CompositeNode` 树操作文档内容，调用 `doc.save()` 时自动同步到 XML。适合大多数应用场景。
- **XML Parts API**：通过 `doc.get_styles()`、`doc.get_document_xml()` 等返回 `pugi::xml_document*`，直接修改底层 XML。适合需要精细控制的高级用户。
- **Legacy Iterator API**：`doc.paragraphs()`、`para.runs()` 等基于 XML 节点的游标式迭代器。仅建议用于旧代码维护。

### 功能分层

| 层级 | 功能 | 实现状态 | 推荐 API |
|------|------|----------|----------|
| **基础** | 文档打开/保存/创建 | 已完成 | DOM / `CDocx` 工厂 |
| **基础** | 段落/Run/表格/单元格的增删改查 | 已完成 | DOM |
| **基础** | 字体、颜色、对齐、行距等基础格式 | 已完成 | DOM |
| **基础** | 页眉/页脚、节属性、页面设置 | 已完成 | DOM |
| **基础** | 项目符号/编号列表 | 已完成 | DOM |
| **中间** | 模板占位符替换 | 已完成 | `Template` / `TemplateEngine` |
| **中间** | 书签文本/图片替换 | 已完成 | `BookmarkReplacer` / `BookmarkInserter` |
| **中间** | 文档插入/合并 | 已完成 | `DocumentInserter` |
| **中间** | 图片添加/删除/替换/导出 | 已完成 | DOM Media API |
| **中间** | DOM ↔ XML 同步（含 Bookmark/Hyperlink/Field） | 已完成 | 自动 |
| **中间** | 节点深拷贝 (`clone`) 与文档级遍历 | 已完成 | DOM |
| **中间** | 表格合并/拆分 | 已完成 | DOM |
| **中间** | 表格 auto-fit、列插入/删除 | 已完成 | DOM |
| **中间** | `DocumentBuilder` 书签跳转、插入图片/超链接/字段 | 已完成 | `DocumentBuilder` |
| **中间** | 全文搜索/替换 | 已完成 | `DocumentSearch` |
| **中间** | 样式系统管理器 | 已完成 | `StyleCollection` |
| **中间** | 批注管理 | 已完成 | `CommentCollection` |
| **中间** | 邮件合并 | 已完成 | `MailMerge` |
| **中间** | 水印 | 已完成 | `Watermark` |
| **高级** | 复杂字段（页码、日期、交叉引用） | 部分实现 | `DocumentBuilder` |
| **高级** | 脚注/尾注 DOM 封装 | 部分实现 | DOM |
| **高级** | PDF / HTML / Markdown 导出 | 未实现 | Roadmap |

### 下一步优先级（推荐）

1. **补全字段便捷方法**：`Paragraph::append_page_number()`、`append_date()` 等
2. **补全页眉页脚便捷方法**：`Section::ensure_header/footer()`
3. **增强样式系统**：自定义样式的创建与修改
4. **远期**：PDF/HTML 导出、Shapes/Charts

---

## 头文件结构

```
include/
├── cdocx.h                      # 主头文件（聚合所有模块）
└── cdocx/
    ├── enums.h                  # 枚举类型
    ├── format.h                 # 格式属性
    │   ├── Color
    │   ├── Border/Borders
    │   ├── Shading
    │   ├── Font
    │   ├── ParagraphFormat
    │   ├── TableFormat
    │   ├── CellFormat
    │   ├── RowFormat
    │   └── PageSetup
    ├── node.h                   # 节点基类
    │   ├── Node
    │   ├── CompositeNode
    │   ├── NodeCollection
    │   └── DocumentVisitor
    ├── document.h               # 文档结构
    │   ├── Document
    │   ├── DocxTree
    │   └── LoadConfig
    ├── cdocx.h (工厂类)        # CDocx 工厂方法
    ├── section.h                # 节和页眉页脚
    │   ├── Section
    │   └── HeaderFooter
    ├── body.h                   # 文档正文
    │   └── Body
    ├── paragraph.h              # 段落和文本
    │   ├── Paragraph
    │   ├── Run
    │   ├── SpecialChar
    │   ├── Field
    │   └── Hyperlink
    ├── paragraph_builder.h      # ParagraphBuilder 流式 API
    ├── table.h                  # 表格
    │   ├── Table
    │   ├── Row
    │   ├── Cell
    │   └── TableCollection
    ├── table_builder.h          # TableBuilder 流式 API
    ├── template.h               # 模板替换 (Legacy FSM)
    ├── template_engine.h        # TemplateEngine 统一模板引擎
    ├── inserter.h               # DocumentInserter 文档插入
    ├── bookmark.h               # Bookmark 集合
    ├── bookmark_replacer.h      # BookmarkReplacer 书签替换
    ├── bookmark_inserter.h      # BookmarkInserter 书签插入
    ├── caption_generator.h      # CaptionGenerator 题注生成
    ├── comment.h                # CommentCollection 批注
    ├── mail_merge.h             # MailMerge 邮件合并
    ├── watermark.h              # Watermark 水印
    ├── style.h                  # Style / StyleCollection 样式
    ├── document_builder.h       # DocumentBuilder 流式构建
    ├── document_search.h        # DocumentSearch 全文搜索替换
    ├── range.h                  # Range 范围操作
    ├── advanced.h               # TableOperations 等高级工具
    ├── convert_util.h           # 单位转换工具
    ├── control_char.h           # 控制字符常量
    ├── file_format_util.h       # 文件格式检测
    ├── footnote.h               # Footnote / FootnoteReference
    ├── formfield.h              # FormField 表单字段
    ├── properties.h             # 属性结构
    ├── constants.h              # 通用常量
    ├── fwd.h                    # 前向声明
    └── iterator.h               # 迭代器支持
```

---

## 参考资源

### TestWord 文档结构

位于 `TestWord/` 目录，包含：

- 段落格式 (对齐、缩进、间距)
- 字符格式 (字体、颜色、大小)
- 表格 (合并单元格)
- 图片
- 书签

### Aspose.Words 参考

位于 `other/asposeWordsCpp/include/`，包含：

- 完整的 API 设计模式
- 1620+ 个头文件
- 表格、字段、样式等完整实现参考

---

## 联系方式

如有问题或建议，请查看 `AGENTS.md` 或提交 Issue。
